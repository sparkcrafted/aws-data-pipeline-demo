# Sample Data
Synthetic inputs for repeatable demos.

- `sample_batch/` — mock CSVs (tiny)
- `sample_stream/` — JSON fixtures for stream tests

> No real data. Keep files small so the repo stays lightweight.
